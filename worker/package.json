{
  "name": "ddo-gear-planner-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply DB --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.0",
    "vitest": "^5.0.1",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "jose": "^5.9.0"
  }
}
